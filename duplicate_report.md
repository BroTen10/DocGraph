# 规则重复检查报告

> 生成时间：2026-07-27  方式：直接查后端 /api/rule-sets + /api/rules
> 去重判定：精确文本(去标点小写) / (文件类型+检查项+文本)三元组 / 同组词重叠≥0.8 近义

## 一、各规则集概览

| 规则集 | 规则数 | 完全相同文本组 | 三元组重复组 | 冗余条目(超额) |
|---|---:|---:|---:|---:|
| 测试规则集2（刚 LLM 文件导入） | 376 | 64 | 74 | **220** |
| 验收-出口代理-24HCSP012260253（种子 30 条） | 30 | 0 | 0 | 0 |
| 出口代理-验证 | 0 | - | - | - |

**结论：导入的「测试规则集2」存在大量明显重复，约 59%（220/376）为冗余条目。**

## 二、Top 重复规则（完全相同文本，按出现次数）

| 次数 | 规则文本(节选) |
|---:|---|
| 22 | 需为双方回签用印版本 |
| 12 | 协议时间覆盖合同时间 |
| 12 | 如合同条款约定详细交货地址与实际交货文件比对地址是否一致… |
| 12 | 入仓单据时间不能早于出仓时间 |
| 10 | 收付款对象是否与系统供应商客户一致，如收款对象不一致看系统是不是有第三方来款说明附件 |
| 10 | 入库数量/采购合同…分批出入库可输出提示待进一步人工确认 |
| 10 | 如系统中该业务发货单中仓库点选了仓库需提供货物信息匹配的出入仓单据 |
| 10 | 单据须有仓库用印 |
| 9 | 收付款对象是否与系统供应商客户一致，如收款对象不一致看系统是否有第三方来款说明附件 |
| 9 | 入库数量不小于出库数量 |
| 9 | 单据上应有合同号关联我司安排物流情况下司机信息… |
| 9 | 单据时间不能早于合同时间 |
| 8 | 收付不得早于协议合同时间 |
| 8 | 先收后付原则，水单时间线上收要早于付 |
| 8 | 单据要有物流公司用印 |
| 7 | 协议需为检查合同对应关联的协议 |
| 7 | 出入仓单据上需有合同号对应的产品信息… |
| 7 | 单据合同号与系统合同号或外部合同号一致 |
| 7 | 合同时间早于合同相关所有流程之前 |
| 6 | 水单现阶段是跟业务部要… |
| 6 | 采购入库时间不得早于采购合同签订时间/销售出库不得早于销售合同签订时间 |
| 6 | 单据内容与系统报关草单界面内容一致 |
| 6 | 单据时间在协议时间范围内不得晚于报关单日期 |
| 6 | 采购合同同规格型号产品数量不小于销售合同数量 |

（共 64 组完全相同文本，以上为次数≥6 的部分）

## 三、近义/高度相似重复（词重叠≥0.8，需人工判断）

这类不是逐字相同，但语义高度接近，部分为「同一条规则换种说法被重复提取」：

- **出口报关单/信息准确性**：`报关单与委托出口确认单信息比对…` ↔ `报关单与销售合同信息比对…` ↔ `报关单与合同信息比对…`（同模式不同目标单据，可能算合理变体，也可能冗余）
- **派车单/时间逻辑**：`如果是国内运输，物流单据时间不晚于报关时间；如果是香港交货…` ↔ `如果仅是国内运输…香港包车运输…`（多处重复出现）
- **出入仓单/时间逻辑**：`入仓单据时间不能早于出仓时间；采购入库时间不得早于采购合同签订时间…` ↔ `采购入库时间不得早于采购合同签订时间…`（前者是后者+前缀，明显重叠）
- **签收单/信息准确性**：`货物签收单上…数量不大于委托出口确认单，等于对应报关单产品数量` ↔ `…数量不大于订单`（近义）
- **采购合同/信息准确性**：`单据合同号与系统合同号…；单据内容与系统制单界面…` ↔ `单据内容与系统制单界面…`（前者是后者+前缀，重叠）

> 注：近义组是否删除需业务判断，本报告**未**自动处理；仅「三元组精确重复」属安全可删。

## 四、根因

文件导入走 `_split_text` 把文档切 11 段、每段独立调 LLM 转规则。当某条规则在多个段落里都被提及（如「双方回签用印」在协议、合同多处出现），各段各自提取 → 同一规则被多次入库，且当前入库逻辑**不做同规则集内去重**。

## 五、建议

1. **立即清理（可选，需你确认）**：对「测试规则集2」按三元组去重，每组保留 1 条、删除其余 → 删 220 条，剩约 156 条。我可以用后端 `DELETE /api/rules?rule_set_id=&ids=` 批量删，不动「验收」集。
2. **根治（改代码）**：在 `rule_import_service.import_rules_from_text` 入库前，按 `(doc_type, check_category, 归一化 rule_text)` 对本次批次做去重（同批只留首条）；更稳的做法是规则集内已有相同三元组则跳过/合并。这样下次再导入不会累积重复。
3. **近义重复**建议人工复核后再决定是否合并，不自动处理。
